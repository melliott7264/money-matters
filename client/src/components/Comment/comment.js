// individual comment form
import React from 'react';

// passing in: (comment)_id, articleId, postDate, username, commentBody
// const Comment = ({ _id, articleId, postDate, username, commentBody }) => (

// );

export default Comment;
